# Root .env.example
# Copy this file to .env in the root directory if needed, or configure envs in server/client directly.
# Update the DATABASE_URL to match your local PostgreSQL instance (e.g. from pgAdmin).

DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/toktickit?schema=public"
PORT=3000
